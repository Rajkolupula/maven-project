pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Rajkolupula/maven-project.git'
            }
        }

        stage('Build') {
            steps {
                withMaven(maven:'Maven_3.9.6') {  
                sh 'mvn clean install'
            }
        }
    }
}
